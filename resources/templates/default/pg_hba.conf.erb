#TYPE   DATABASE        USER            CIDR-ADDRESS            METHOD
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust
<% @routes.each do |r| %>
host    template1       postgres        <%= r.to_s %>         trust
host    postgres        postgres        <%= r.to_s %>         trust
host    replication     postgres        <%= r.to_s %>         trust
host    opscode_chef    postgres        <%= r.to_s %>         trust
host    druid           druid           <%= r.to_s %>         trust
host    redborder       redborder       <%= r.to_s %>         trust
<% end %>

